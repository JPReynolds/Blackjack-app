import React from 'react';

const SetUp = () => {
  return (
    <div>
      <img
        src={require('../images/facedowncard.png')}
        alt="facedowncard"
        className="card"
      />
      <img
        src={require('../images/facedowncard.png')}
        alt="facedowncard"
        className="card"
      />
    </div>
  );
};

export default SetUp;
